import styled, { ThemeProvider } from 'styled-components';

const buttonCommon = {
    fontWeight: "bold",
    borderRadius: "3px",
}

const inputCommont = {
    backgroundClor: "#fff",
    // placeHolder: "#ccc",
}

const colors = {
    black: "#222",
    darkgray: "#444a4c",
    ligthgray: "#a7a9af",
    orange: "rgba(242, 113, 28, 0.85)",
    yellow: "#ffca61",
}

const theme = {
    buttonCommon,
    colors,
};

export default theme;